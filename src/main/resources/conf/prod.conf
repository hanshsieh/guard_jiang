include "common.conf"

sqlReader = ${sqlReader} {
    passwordKeyName = "org.guard_jiang.db.prod.pwd"
}

sqlWriter = ${sqlWriter} {
    passwordKeyName = "org.guard_jiang.db.prod.pwd"
}

secret = ${secret} {
    path = "secret/prod.properties"
}
